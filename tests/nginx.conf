server {
                listen 80;
                server_name test.frezbo.com;
                root /var/www/html;
                index index.html;

        location / {
                        try_files \$uri =404;
                }

        location ~ .well-known/ {
                        try_files \$uri =404;
                        log_not_found off;
                }
}
